/*Write a C program to find the power of integers. */
#include<stdio.h>
#include<math.h>
int main(){
    int a,b;
    scanf("%d",&a);
    scanf("%d",&b);
    int c=pow(a,b);
    printf("%d",c);
    
}
